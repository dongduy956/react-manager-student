export const searchSelector = (state) => state.search.searchText;
export const authActiveSelector = (state) => state.auth.authActive;
export const pathUpdateAuthSelector = (state) => state.auth.pathUpdate;
